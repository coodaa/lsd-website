name: 🚀 Deploy to STRATO via SFTP

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      WP_API: ${{ secrets.WP_API }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🧱 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧰 Build project
        run: npm run build

      - name: 🚀 Upload to STRATO via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          server: ssh.strato.de
          port: 2022           # ← WICHTIG: Strato blockt GitHub auf Port 22
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          local_path: ./dist/*
          remote_path: /www-lsd/
          sftp_only: true
          delete_remote_files: false
