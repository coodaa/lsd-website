name: 🚀 Deploy to STRATO via SFTP

on:
  push:
    branches:
      - main   # läuft bei jedem Push auf 'main'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Code auschecken
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Node.js einrichten
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Dependencies installieren
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Projekt builden
      - name: Build project
        run: npm run build

      # 5️⃣ Upload via echtes SFTP (Strato)
      - name: Upload via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          server: ssh.strato.de
          port: 22
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          local_path: ./dist/
          remote_path: /www-lsd/   # ← das ist jetzt korrekt!
          sftp_only: true
          delete_remote_files: false

      - name: Build project
        run: npm run build
        env:
          WP_API: ${{ secrets.WP_API }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      WP_API: ${{ secrets.WP_API }}
